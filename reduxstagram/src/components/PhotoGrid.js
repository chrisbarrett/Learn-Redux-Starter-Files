// @flow
import React from 'react';

type Props = {| |}

class PhotoGrid extends React.Component {
  static propTypes = {};

  props: Props;

  render = () => {
    return (
      <p>Photo grid</p>
    )
  };
}

export default PhotoGrid;
